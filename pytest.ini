[pytest]
# Without this, plain `pytest` cannot import llmeval.*; only `python -m pytest`
# works, because that puts the working directory on sys.path. The two must
# agree, since the gate runs one and developers run the other.
pythonpath = .
testpaths = tests
