.env
.env.local
venv/
.venv/
__pycache__/
*.pyc
.pytest_cache/
.deepeval/
.DS_Store
reports/*
!reports/.gitkeep
# Committed on purpose: the measurement the README quotes. A detector accuracy
# figure nobody can reproduce is a claim, not a result.
!reports/detector_accuracy_run*.json
# A local vocabulary override at the repo root (see LLMEVAL_PATTERNS).
# Anchored, so the committed fixture at data/grounding/patterns.json still ships.
/patterns.json

# harness template state
.template.env
.template.modules
.claude/logs/

# Local editor/assistant config; may contain API tokens
.mcp.json
